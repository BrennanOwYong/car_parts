interface:
  display_name: "Vehicle Schematic Sourcing"
  short_description: "Find OEM geometry, then public scans"
  default_prompt: "Use $vehicle-schematic-sourcing to check OEM geometry, then public 3D scans, and request LiDAR only if both are insufficient."
