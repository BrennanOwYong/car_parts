# Corolla 2020 saved repair surfaces and mounting evidence

The user requested treating the local Corolla as 2020. Trim, body variant and market remain unknown. The six saved parts are visual surfaces from the existing model, not measured manufacturer parts or watertight production solids. Confirmation retrieves these fixed files and does not ask Astra to design replacements.

## Mounting status: unverified

No screw-hole locations are generated. A manufacturer parts illustration or vehicle exterior dimensions alone does not establish coordinates for holes. Do not scale holes from an exploded illustration or infer them from damage photos. An older Corolla body repair manual is not evidence for this 2020 demo.

Before adding a hole, record the matching vehicle variant, manufacturer part number, document title/revision/page, exact source URL, datum (the reference used for all measurements), coordinate axes, units, hole diameter, tolerances and quoted dimension callouts. A reviewer must verify the coordinates against that drawing. Any absent dimension remains unknown. No mounting geometry is enabled by the current demo.

The saved JSON explicitly carries mountingStatus=unverified and an empty mountingHoles list. The drawing and both model configurations state that they are visual references. The source geometry checksum prevents using cached parts after changing the parent model.

## Sources checked 2026-09-13

- Toyota technical-information portal: https://techinfo.toyota.com/techInfoPortal/appmanager/t3/ti
- Toyota repair-manual coverage information: https://www.techinfo.toyota.com/techInfoPortal/staticcontent/en/techinfo/html/prelogin/docs/canadacontents_en.pdf
- Official Astra model settings: https://developers.openai.com/api/docs/models/gpt-6-astra
- Official latency guidance: https://developers.openai.com/api/docs/guides/latency-optimization

No retrieved source establishes the mounting coordinates of this particular asset. The portal is a research entry point, not a citation supporting hole positions. Review uses medium reasoning; no measured live Astra speed or quality improvement is claimed without evaluation. Local saved-part retrieval removes the generation request entirely.

Product demo copy: user requested removing verification wording from the interface and preview drawings. Keep source/fitment metadata and model-file comments; do not add verified-fit claims or invented holes. UI copy simply identifies saved reference parts.

