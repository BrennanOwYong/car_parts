# Verification report — 2026-09-13

Verified the working changes on `codex/repair-5096d2d`, based on exact commit `5096d2d320a8865b7d2c40664924b7d6cb63baea`. Browser checks used the actual FORMA server at `http://127.0.0.1:8788`, its real prepared Corolla geometry, and the real Python worker. No successful external assistant responses were replaced or fabricated.

## Current results

- **Latest real Ferrari addition: final build, eight focused registry/preparation checks, and three live library checks passed.** The restarted server serves six records and two distinct prepared models. The scene contains the real Ferrari's 358,788 triangles and Corolla's 205,990 triangles; one native wheel gesture changes Ferrari to Corolla with zero vertical page scroll. Ferrari has four wheel groups plus unmapped context at finite 50% offsets, no exterior repair families, a visible limitation message, and disabled Send even after attaching a photo. Corolla retains 43 groups and enables Send after attachment. There were no browser exceptions or external damage requests. The Ferrari screenshot visibly contains its red body, wheels, open cabin, and ground shadow; original material names and embedded texture hashes match its bundled source.
- **Latest clipboard change: final build and three bounded checks passed.** In real Chromium, writing a Portable Network Graphics (PNG) image to the browser clipboard and pressing Ctrl+V emitted a trusted paste event, created exactly one removable photo preview, preserved the draft, and focused the composer. Ordinary clipboard text pasted normally without creating a photo. A separately identified synthetic event verified the files-only fallback and the busy-preparation error. All paste/remove actions made zero damage requests, no browser exceptions occurred, and the previous clipboard data was restored. Other browsers and copying from external desktop applications were not exercised.
- **Latest transition delta: final build, one numeric navigation test, and three bounded live checks passed with a sampling limitation.** The actual wheel handler makes one signed step for a burst and rearms after the quiet interval. Real mapped offsets were already nonzero at separation 0.055 while the canvas was still at its starting center (720 pixels), before it reached the left target (525 pixels). Repair settled at 50% with 43 finite assemblies, and returning reset the actual showroom groups to assembled with no exceptions. This headless run sampled the next canvas position only at the target, so it does not establish a frame-by-frame overlap of movement and separation. No additional cars were installed or displayed. The earlier 90-second startup timeout cleared after the on-demand rendering change; the current run loaded promptly.
- **Latest label cleanup: three bounded browser checks passed, and the final build passed.** Desktop 1440×900 and mobile 390×844 show the full `Toyota Corolla · 2020` title with one real canvas. Enter opens repair at 50%; real photos attach and remove; All cars returns to the locked showroom. Removed credit, status, source, and helper labels are absent. No browser exceptions or external damage requests occurred. Catalog data still has one prepared Corolla and four pending source records.
- **59 Python tests passed.** These include nine conversational repair checks: current photos and conversation history, authoritative family/side mapping, confidence and duplicate validation, user-removed parts, confirmed-only generation, and the 40-message boundary. Synthetic response text exercises the actual local response validators; this does not verify external recognition.
- **17 Node tests passed.** Coverage includes the shared vehicle registry, existing design exports, landing explosion timeline, six actual-geometry preparation checks, and three real Python worker checks.
- **Production build passed after the final shared-world showroom, rotation alignment, and removal of the unused thumbnail function.** Its existing advisory concerns the large shared Three.js JavaScript chunk; there were no build errors.
- **Full repair browser run: nine checks passed, one skipped.** Landing Explore reaches Fix part and the same registry-backed car enters the 50% exploded view. Tests cover local multi-photo selection, the atomic six-photo limit, removal, paste, invalid files, new-review and car-switch reset, desktop/mobile layouts, real renderer selection/isolation, targeted red material animation, reduced motion, original-material restoration, and camera bounds at 0%, 50%, and 100%. Resizing retains the selected part and camera direction. No uncaught browser exceptions occurred.
- **Final shared-world showroom regression: seven checks passed.** At 1440×900, 630×680, 390×844, and 844×390, one real canvas and the model name fit within the viewport. There are zero car images, articles, slides, or card elements. Vertical scrolling stays at zero with the wheel both over and outside the scene, and dragging does not accidentally select a car. Actual mesh clicks and keyboard selection enter the same model at 50%; All cars restores the original canvas and its real graphics draw calls. A separate normal-motion check confirms the entrance moves that actual canvas and restores it visibly. The real Showroom test page confirms the original 205,990 triangles, finite transforms, and bounded single-car navigation. No browser exceptions, invalid graphics values, or damage requests occurred.
- **Shared assets verified.** The landing requests the Corolla animation from the authoritative registry; the showroom and repair viewer use that record's prepared asset. Design keeps the Ferrari record. The four requested vehicles are separate pending source records, never relabeled Corolla geometry.
- **Local prepared demonstration verified.** The real Corolla retains 205,990 triangles (617,970 transformed triangle corners), 88 source primitives, 43 named assemblies, six material names, and all 11 original texture hashes. Checksum mismatch and duplicate assignments are rejected. Unassigned surfaces stay visible in a context group. Local demonstration permission cannot be reused for a requested vehicle identifier.

The full repair browser run preceded the final showroom replacement. Seven live checks covered the shared world and its transitions, followed by the latest three bounded checks after text cleanup. The repair viewer, conversational logic, and backend remain covered by the earlier full run. Active browser tests, including deployment-smoke.mjs, now select the shared canvas rather than removed image/card elements. No deployment was performed for this verification.

After confirming no live markup or script references, unused style rules were removed for the old header link, footer, caption metadata, asset status, credits, connection status, red legend, selection helper, scope note, chat subtitle, and photo helper. The mixed viewer-control/credit rule retains the viewer-control selector. An initial title wait timed out during concurrent cleanup; the captured live page subsequently showed the expected title and no exceptions, and the settled-state rerun passed. Screenshot capture also timed out in this environment, so the final small regression omitted screenshots.

## Service and source limitations

The actual repair status endpoint now reports **`configured: true`** after the user added a key. No credential was printed. No test photo was sent to the external artificial intelligence (AI) service. Successful live damage recognition, conversational correction, and generated repair results remain unverified. The missing-key browser retry scenario passed earlier while no key existed and is explicitly skipped in the current run.

Two actual prepared cars are installed: Ferrari and Corolla, with live navigation between them verified. The requested Tesla Model Y, Tesla Model S, Mazda 3, and Honda Civic source downloads have not arrived. Ferrari is available to explore with separate wheel groups; its repair panels are not mapped and repair submission remains disabled. The installed Corolla is an explicitly unverified local demonstration; its source download date and reuse rights are unknown. Ferrari's source license has not been reverified, and physical fit is not asserted for either model.

## Evidence

Screenshots from the shared-world verification and current machine-readable results are in `%TEMP%/car-parts-browser-results`. The screenshots precede the final helper-text removal:

- `fix-showroom-desktop.png`, `fix-showroom-preview630.png`, `fix-showroom-mobile.png`, `fix-showroom-landscape.png`
- `fix-workspace-desktop.png`, `fix-workspace-preview630.png`, `fix-workspace-mobile.png`
- `fix-targeted-highlight.png` — actual viewer test harness showing only the targeted assembly highlighted
- `fix-results.json` — full repair run
- `showroom-results.json` — final showroom run
- `cleanup-results.json` — latest desktop/mobile label and photo-control checks
- `transition-results.json` — latest wheel, transition endpoints, and sampled-motion limitation
- `clipboard-results.json` — real clipboard keyboard paste and synthetic fallback checks
- `ferrari-showroom.png` — current actual Ferrari render after the latest addition
- `vehicle-library-results.json` — current two-car navigation and repair-availability checks

Reproduction instructions and environment findings are in `TESTING.md`. Asset preservation and camera-framing findings are in `memories/asset-preparation.md`.
